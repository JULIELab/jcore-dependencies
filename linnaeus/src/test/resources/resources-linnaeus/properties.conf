$dir = internal:/resources-linnaeus

variantMatcher=$dir/species-light.tsv.gz
 
postProcessing
ppStopTerms=$dir/stoplist.tsv
ppAcrProbs=$dir/synonyms-acronyms.tsv
ppEntityFreqs=$dir/species-frequency.tsv.gz

